<Window x:Class="ClamHub.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About ClamHub" Height="320" Width="440"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        Background="{StaticResource BgBrush}"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border BorderBrush="{StaticResource WindowBorderBrush}" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="ClamHub" FontSize="24" FontWeight="SemiBold"/>
            <StackPanel Grid.Row="1" Margin="0,6,0,0">
                <TextBlock x:Name="HeaderVersionLine" Foreground="{StaticResource MutedTextBrush}"/>
                <TextBlock x:Name="DbHeaderLine" Margin="0,10,0,3"
                           Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap"/>
                <TextBlock x:Name="DbListLine" FontFamily="Consolas" FontSize="12"
                           Foreground="{StaticResource MutedTextBrush}"/>
            </StackPanel>
            <TextBlock Grid.Row="2" Margin="0,16,0,0" TextWrapping="Wrap"
                       Text="A portable, modern desktop frontend for ClamAV."/>

            <TextBlock Grid.Row="4" VerticalAlignment="Bottom">
                <Hyperlink NavigateUri="https://github.com/b7i6gf/ClamHub"
                           RequestNavigate="Hyperlink_RequestNavigate"
                           Foreground="{StaticResource AccentBrush}">
                    GitHub Repository
                </Hyperlink>
            </TextBlock>

            <Button Grid.Row="4" Content="Close" Width="80" Height="30"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Click="Close_Click"/>
        </Grid>
    </Border>
</Window>
